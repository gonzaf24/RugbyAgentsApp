const accessKeyId = "AKIAIB3ACXUXJ24LTSBA";
const secretAccessKey = "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";
const region = "eu-west-3";
const bucketName = "rugbyagents";

export const DOCUMENTOS_PLAYER = {
  bucketName: bucketName,
  dirName: "documentosPlayer",
  region: region,
  accessKeyId: accessKeyId,
  secretAccessKey: secretAccessKey,
};

export const DOCUMENTOS_CLUB = {
  bucketName: bucketName,
  dirName: "documentosClub",
  region: region,
  accessKeyId: accessKeyId,
  secretAccessKey: secretAccessKey,
};

export const PERFIL_PLAYER = {
  bucketName: bucketName,
  dirName: "perfilPlayer",
  region: region,
  accessKeyId: accessKeyId,
  secretAccessKey: secretAccessKey,
};

export const PERFIL_CLUB = {
  bucketName: bucketName,
  dirName: "perfilClub",
  region: region,
  accessKeyId: accessKeyId,
  secretAccessKey: secretAccessKey,
};

export const DOCUMENTOS_AGENT = {
  bucketName: bucketName,
  dirName: "documentosAgent",
  region: region,
  accessKeyId: accessKeyId,
  secretAccessKey: secretAccessKey,
};

export const PERFIL_AGENT = {
  bucketName: bucketName,
  dirName: "perfilAgent",
  region: region,
  accessKeyId: accessKeyId,
  secretAccessKey: secretAccessKey,
};
